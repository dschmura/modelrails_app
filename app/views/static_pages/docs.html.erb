<embed src="https://dschmura.github.io/modelrails_app/" class="w-screen  min-h-screen " id="docs"/> 


<%# <iframe src="/documentation" class="w-screen max-w-6xl min-h-screen " id="docs"></iframe> %>

<%# Passes submitted url to iframe so that proper pages are displayed. %>
<script> // Inside the parent window's JavaScript code
// Inside the parent window's JavaScript code
document.addEventListener('click', function(event) {
  var target = event.target;
  if (target.tagName === 'A') {
    // event.preventDefault(); // Prevent the default link behavior

    var iframe = document.getElementById('docs'); // Replace with your iframe's ID
    var newUrl = target.href;

    var message = {
      params: {
      }
    };

    // Send the message containing parameters to the iframe
    iframe.contentWindow.postMessage(message, newUrl);
  }
});

 </script>